# Apache Spark worker image for Docker

This image is a worker for a Spark cluster.

# Build

* ```docker build -t <name>/spark-worker .```

# Use

* ```docker run -d <name>/spark-worker <master_ip>```
